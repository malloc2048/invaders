#include "logical_group.h"
#include "../stack_io_machine_group/stack_io_machine_group.h"

namespace rotate {
    void RLC(Registers& registers) { machine::Unimplemented(registers); }
    void RRC(Registers& registers) { machine::Unimplemented(registers); }
    void RAL(Registers& registers) { machine::Unimplemented(registers); }
    void RAR(Registers& registers) { machine::Unimplemented(registers); }
}
